pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'echo "Testing Jenkins Pipeline"'
                bat 'python --version'
            }
        }
    }
}
